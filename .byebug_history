continue
pp @entrants.limit(2).map {|x| x.attributes}
require "pp"
pp @entrants.limit(2).map {|x| x.attributes}
continue
pp @entrants.limit(2).map {|x| x.attributes}
require "pp"
pp @entrants.limit(2).map {|x| x.attributes}
continue
pp @entrants.limit(2).map {|x| x.attributes}
require "pp"
@entrants.limit(2).map {|x| x.attributes}[1]
@entrants.limit(2).map {|x| x.attributes}
a
a = @entrants.limit(2).map {|x| x}
@entrants.limit(2).map {|x| x}
pp @entrants.limit(2).map {|x| x}
@entrants.limit(2).map {|x| x}
@entrants.limit(2).all
@entrants.limit(2)
@entrants.count
@entrants
